# 文件说明和使用指南

## 📦 项目文件列表

### 核心Java代码（放在 app/src/main/java/com/example/hitokotowidget/）
1. **HitokotoWidget.java** - Widget主类，负责生命周期和定时更新
2. **HitokotoService.java** - 网络请求服务（推荐使用）
3. **HitokotoServiceV2.java** - 改进版服务（可选，使用更现代的Handler实现）
4. **HitokotoData.java** - 数据模型类

### 布局文件（放在 app/src/main/res/layout/）
5. **widget_layout.xml** - Widget的UI布局

### 样式文件（放在 app/src/main/res/drawable/）
6. **widget_background.xml** - Widget背景样式

### 配置文件（放在 app/src/main/res/xml/）
7. **widget_info.xml** - Widget配置信息

### 资源文件（内容添加到 app/src/main/res/values/strings.xml）
8. **strings.xml** - 字符串资源

### 项目配置
9. **AndroidManifest.xml** - 应用清单文件（替换默认的）
10. **build.gradle** - Gradle构建配置（替换app目录下的）

### 文档
11. **README.md** - 完整开发文档
12. **QUICKSTART.md** - 快速开始指南（推荐先看这个）

## 🚀 快速开始三步走

### 第一步：创建项目
在Android Studio中创建新项目：
- 选择 "Empty Views Activity"
- Package name: com.example.hitokotowidget
- Minimum SDK: API 21

### 第二步：复制文件
按照以下路径复制文件：
```
app/
├── src/main/
│   ├── java/com/example/hitokotowidget/
│   │   ├── HitokotoWidget.java
│   │   ├── HitokotoService.java
│   │   └── HitokotoData.java
│   ├── res/
│   │   ├── drawable/
│   │   │   └── widget_background.xml
│   │   ├── layout/
│   │   │   └── widget_layout.xml
│   │   ├── values/
│   │   │   └── strings.xml (合并内容)
│   │   └── xml/
│   │       └── widget_info.xml (新建文件夹)
│   └── AndroidManifest.xml (替换)
└── build.gradle (替换)
```

### 第三步：运行
1. Sync Project with Gradle Files
2. Run App
3. 在桌面添加Widget

## ⚡ 关键配置点

### 1. 小米手机必须设置
- ✅ 允许自启动
- ✅ 允许后台运行（省电策略设为"无限制"）
- ✅ 允许网络权限

### 2. 创建xml文件夹
在 `app/src/main/res/` 下手动创建 `xml` 文件夹（如果不存在）

### 3. 同步Gradle
复制文件后，务必点击 "Sync Project with Gradle Files"

## 🔧 技术实现要点

### API接口
```
GET https://api.codelife.cc/yiyan/random?lang=cn

返回格式：
{
  "code": 200,
  "data": {
    "hitokoto": "一句话内容",
    "from": "作者或来源"
  },
  "msg": "加载成功"
}
```

### 更新机制
- 使用 AlarmManager 实现每小时定时更新
- 点击Widget可立即刷新
- 使用 IntentService 在后台执行网络请求

### Widget尺寸
- 配置为 4x2 （250dp x 110dp）
- 支持横向和纵向调整大小

## 📝 常见问题

**Q: Widget不显示？**
A: 检查widget_info.xml是否在res/xml目录下

**Q: 显示"加载中"不更新？**
A: 检查网络权限和后台运行权限

**Q: 1小时后不自动更新？**
A: 小米手机需要允许自启动和关闭省电限制

## 📚 详细文档

- **QUICKSTART.md** - 5分钟快速部署指南
- **README.md** - 完整的技术文档和优化建议

## 🎯 下一步建议

1. 先运行基础版本，确保功能正常
2. 根据需要进行UI美化
3. 添加深色模式支持
4. 实现本地缓存功能
5. 使用WorkManager替代AlarmManager（更省电）

## ⚠️ 注意事项

1. **网络请求在后台线程**：已使用IntentService处理
2. **Android 6.0+权限**：需要动态申请网络权限（本项目已在Manifest声明）
3. **MIUI优化**：必须在系统设置中允许后台运行
4. **API稳定性**：建议添加错误重试机制

祝开发顺利！🎉
